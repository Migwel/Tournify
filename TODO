- Stop using eager fetching
- Create SQL files for schemas (Use Flyway?)
- Write tests
- Create docker image for easy use
- Create an Open API for easier integration
- Create example requests
- Deletion of subscription should be more secure
- Give option to use certificate for notifications
- Add some kind of domain penalty-box for notifications otherwise Tournify can be used to DOS a domain
- Make communication.jar available on maven so that people can use it to handle requests and responses instead of creating objects themselves